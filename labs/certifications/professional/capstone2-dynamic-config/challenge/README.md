# 🎯 Challenge: Pro · Objective 2: dynamic configuration and troubleshooting

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone2-dynamic-config` proves the final state (`terraform show -json`), idempotence and a clean destroy.
