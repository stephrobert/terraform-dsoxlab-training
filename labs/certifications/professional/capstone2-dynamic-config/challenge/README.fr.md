# 🎯 Challenge : Pro · Objectif 2 : configuration dynamique et troubleshooting

## ✅ Objectif

<!-- TODO : énoncer ce que l'apprenant doit réaliser, dans `challenge/work`. -->

Squelette à compléter (`main.tf`) :

```hcl
# TODO : remplacer chaque ??? par la configuration attendue.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone2-dynamic-config` prouve l'état final (`terraform show -json`), l'idempotence et un destroy propre.
