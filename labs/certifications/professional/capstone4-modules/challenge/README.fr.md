# 🎯 Challenge : Pro · Objectif 4 : créer, maintenir et utiliser des modules

## ✅ Objectif

<!-- TODO : énoncer ce que l'apprenant doit réaliser, dans `challenge/work`. -->

Squelette à compléter (`main.tf`) :

```hcl
# TODO : remplacer chaque ??? par la configuration attendue.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone4-modules` prouve l'état final (`terraform show -json`), l'idempotence et un destroy propre.
