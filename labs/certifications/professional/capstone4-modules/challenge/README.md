# 🎯 Challenge: Pro · Objective 4: create, maintain and use modules

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone4-modules` proves the final state (`terraform show -json`), idempotence and a clean destroy.
