# 🎯 Challenge : Pro · Objectif 3 : workflows collaboratifs

## ✅ Objectif

<!-- TODO : énoncer ce que l'apprenant doit réaliser, dans `challenge/work`. -->

Squelette à compléter (`main.tf`) :

```hcl
# TODO : remplacer chaque ??? par la configuration attendue.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone3-collaborative-workflows` prouve l'état final (`terraform show -json`), l'idempotence et un destroy propre.
