# 🎯 Challenge: Pro · Objective 6: HCP Terraform (multiple-choice)

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone6-hcp` proves the final state (`terraform show -json`), idempotence and a clean destroy.
