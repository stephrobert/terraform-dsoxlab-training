# 🎯 Challenge : Pro · Objectif 1 : cycle de vie, import et réconciliation de drift

## ✅ Objectif

<!-- TODO : énoncer ce que l'apprenant doit réaliser, dans `challenge/work`. -->

Squelette à compléter (`main.tf`) :

```hcl
# TODO : remplacer chaque ??? par la configuration attendue.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check certifications-professional-capstone1-resource-lifecycle` prouve l'état final (`terraform show -json`), l'idempotence et un destroy propre.
