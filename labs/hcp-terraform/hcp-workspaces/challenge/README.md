# 🎯 Challenge: Workspaces Hcp (skeleton)

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check hcp-terraform-hcp-workspaces` proves the final state (`terraform show -json`), idempotence and a clean destroy.
