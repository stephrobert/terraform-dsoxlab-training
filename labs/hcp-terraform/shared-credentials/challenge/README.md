# 🎯 Challenge: Credentials Partage (skeleton)

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check hcp-terraform-shared-credentials` proves the final state (`terraform show -json`), idempotence and a clean destroy.
