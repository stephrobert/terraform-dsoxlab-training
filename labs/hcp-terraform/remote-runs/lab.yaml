---
id: hcp-terraform-remote-runs
title: "The stream a run sends back, and the three ways to start one"
description: |
  What a CLI receives from a remote run is the structured stream `terraform apply -json`
  also produces locally. Record it, analyse it in HCL — two `change_summary` messages, only
  one of which says what happened — and establish what each of the three workflows allows.
section: hcp-terraform
level: l3
difficulty: advanced
lab_type: lab
estimated_time: 35m
skills:
- terraform
- remote-runs
distros:
- debian13
doc_url: https://blog.stephane-robert.info/docs/infra-as-code/provisionnement/terraform/hcp-terraform/remote-runs/
certification_tags:
- tf-professional
runtime:
  type: shell
  workdir: challenge/work
  fixtures:
  - flux/main.tf
  - analyse/versions.tf
  - analyse/analyse.tf
  - questionnaire/versions.tf
  - questionnaire/questionnaire.tf
  - questionnaire/reponses.auto.tfvars
validation:
  functional: true
  security: false
  persistence_after_reboot: false
