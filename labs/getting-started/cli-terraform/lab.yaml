---
id: getting-started-cli-terraform
title: Make fmt, validate and the outputs agree
description: |
  Three faults are planted: a file outside canonical format, an orphan reference that fails `validate`, and missing outputs. Fix all three without changing what the configuration produces, then prove an expression recomputes outside the state.
section: getting-started
level: l1
difficulty: beginner
lab_type: lab
estimated_time: 30m
skills:
- terraform
- cli
distros:
- debian13
doc_url: https://blog.stephane-robert.info/docs/infra-as-code/provisionnement/terraform/decouvrir/cli-terraform/
certification_tags:
- tf-associate
runtime:
  type: shell
  workdir: challenge/work
  # Sans cette liste, AUCUNE fixture n'est posée : `dsoxlab run`
  # laisse le workdir VIDE, et des tests peuvent alors passer sur
  # un répertoire vide. Mesuré le 2026-09-18 : 2 tests sur 5
  # verts sans un seul fichier.
  fixtures:
  - versions.tf
  - main.tf
validation:
  functional: true
  security: false
  persistence_after_reboot: false
