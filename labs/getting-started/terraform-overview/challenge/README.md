# 🎯 Challenge: Presentation Terraform (skeleton)

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check getting-started-terraform-overview` proves the final state (`terraform show -json`), idempotence and a clean destroy.
