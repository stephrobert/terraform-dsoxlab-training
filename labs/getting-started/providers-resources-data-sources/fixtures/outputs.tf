# A COMPLETER.
#
# Deux sorties, qui doivent venir de DEUX natures differentes : l'une d'un objet
# que Terraform a cree, l'autre de ce qu'il s'est contente de lire.

# A COMPLETER : l'identifiant produit par le tirage au sort.
output "empreinte" {
  value = ???
}

# A COMPLETER : le contenu lu dans `catalogue.txt`.
output "catalogue_lu" {
  value = ???
}
