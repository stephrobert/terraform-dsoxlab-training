---
id: state-backends
title: "Migrate the state without breaking its lineage"
description: |
  A backend block accepts no named value. Move from an implicit local backend to a
  parameterised one through partial configuration and `-backend-config`, and prove the
  state migrated rather than being recreated: same `lineage`, both resources still
  managed.
section: state
level: l2
difficulty: intermediate
lab_type: lab
estimated_time: 30m
skills:
- terraform
- backends
distros:
- debian13
doc_url: https://blog.stephane-robert.info/docs/infra-as-code/provisionnement/terraform/state/backends-terraform/
certification_tags:
- tf-associate
- tf-professional
runtime:
  type: shell
  workdir: challenge/work
  fixtures:
  - versions.tf
  - variables.tf
  - main.tf
  - dev.local.tfbackend
validation:
  functional: true
  security: false
  persistence_after_reboot: false
