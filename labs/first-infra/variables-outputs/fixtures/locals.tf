# A completer : le nom de la pile.
#
# Il vaut `app-<env>-<region>`, CALCULE en interne. Personne ne le fournit de
# l'exterieur : c'est ce qui distingue un `local` d'une variable.
locals {
  stack_name = ???
}
