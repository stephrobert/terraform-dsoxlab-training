# A completer : le manifeste.
#
# Un `local_file` nomme `manifest-<stack_name>.json`, dont le contenu decrit
# l'environnement, la region, le sizing et les replicas.
resource "local_file" "manifest" {
  ???
}
