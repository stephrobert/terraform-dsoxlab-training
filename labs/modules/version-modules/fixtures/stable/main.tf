# Ce projet suit la version mineure 1.1.0, et profite de la nouveaute qu'elle
# apporte : l'etiquette doit porter le suffixe « nord ».
#
# L'argument `version` ci-dessous vient d'un copier-coller depuis un bloc de
# registre. Il n'a aucun sens ici, et l'init le refuse.
module "etiquette" {
  source  = "???"
  version = "1.1.0"

  prefixe = "atelier"
}
