# 🎯 Challenge : Import Moved Drift (squelette)

## ✅ Objectif

<!-- TODO : énoncer ce que l'apprenant doit réaliser, dans `challenge/work`. -->

Squelette à compléter (`main.tf`) :

```hcl
# TODO : remplacer chaque ??? par la configuration attendue.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check aws-import-moved-drift` prouve l'état final (`terraform show -json`), l'idempotence et un destroy propre.
