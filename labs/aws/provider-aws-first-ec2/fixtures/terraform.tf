terraform {
  required_version = ">= 1.11.0"

  required_providers {
    aws = {
      # D'ou vient le provider, et quelle plage de versions accepte-t-on ?
      source  = "???"
      version = "???"
    }
  }
}
