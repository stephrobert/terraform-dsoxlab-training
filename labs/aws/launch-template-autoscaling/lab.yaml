---
id: aws-launch-template-autoscaling
title: Read a replacement in the plan, before it happens
description: 'An ASG referencing `version = "$Latest"` never triggers an instance refresh, and a `create_before_destroy` placed on the launch template protects nothing. Read both truths in the plan converted
  to JSON, without ever calling AWS.

  '
section: aws
level: l3
difficulty: advanced
lab_type: lab
estimated_time: 30m
skills:
- terraform
- launch-template-autoscaling
distros:
- debian13
doc_url: https://blog.stephane-robert.info/docs/infra-as-code/provisionnement/terraform/aws/launch-template-autoscaling/
certification_tags:
- tf-professional
runtime:
  type: shell
  workdir: challenge/work
  fixtures:
  - versions.tf
  - main.tf
  - CONSIGNES.md
  - terraform.tfstate
validation:
  functional: true
  security: false
  persistence_after_reboot: false
