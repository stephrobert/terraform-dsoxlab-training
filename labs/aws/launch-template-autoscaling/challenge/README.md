# 🎯 Challenge: Launch Template Autoscaling (skeleton)

## ✅ Objective

<!-- TODO: state what the learner must achieve, in `challenge/work`. -->

Skeleton to complete (`main.tf`):

```hcl
# TODO: replace every ??? with the expected configuration.
resource "???" "???" {
  ??? = ???
}
```

## 🔍 Validation

`dsoxlab check aws-launch-template-autoscaling` proves the final state (`terraform show -json`), idempotence and a clean destroy.
